{
  "startCommand": "php spark serve --host=0.0.0.0 --port=8080",
  "buildCommand": "composer install --no-interaction --prefer-dist",
  "installCommand": "composer install --no-interaction --prefer-dist"
}
